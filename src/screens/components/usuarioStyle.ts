import { StyleSheet } from "react-native";

export const stylesUsuario = StyleSheet.create({
    container: {
        padding: 16,
        backgroundColor: "#FFF",
        borderRadius: 8,
        marginBottom: 8,
        elevation: 3,
    },
    name: {
        fontSize: 18,
        fontWeight: "bold",
        color: "#333",
    },
    email: {
        fontSize: 14,
        color: "#666",
        marginTop: 4,
    },
});
